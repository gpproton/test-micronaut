apiVersion: apps/v1
kind: Deployment
metadata:
  name: "service-pipeline"
spec:
  selector:
    matchLabels:
      app: "service-pipeline"
  template:
    metadata:
      labels:
        app: "service-pipeline"
    spec:
      containers:
        - name: "service-pipeline"
          image: "gcr.io/service-pipeline/jib-image"
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
            failureThreshold: 10
---
apiVersion: v1
kind: Service
metadata:
  name: "service-pipeline"
spec:
  selector:
    app: "service-pipeline"
  type: LoadBalancer
  ports:
    - protocol: "TCP"
      port: 8080