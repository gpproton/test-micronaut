apiVersion: apps/v1
kind: Deployment
metadata:
  name: "pipe"
spec:
  selector:
    matchLabels:
      app: "pipe"
  template:
    metadata:
      labels:
        app: "pipe"
    spec:
      containers:
        - name: "pipe"
          image: "gcr.io/pipe/jib-image"
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
            failureThreshold: 10
---
apiVersion: v1
kind: Service
metadata:
  name: "pipe"
spec:
  selector:
    app: "pipe"
  type: LoadBalancer
  ports:
    - protocol: "TCP"
      port: 8080