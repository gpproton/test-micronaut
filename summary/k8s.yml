apiVersion: apps/v1
kind: Deployment
metadata:
  name: "summary"
spec:
  selector:
    matchLabels:
      app: "summary"
  template:
    metadata:
      labels:
        app: "summary"
    spec:
      containers:
        - name: "summary"
          image: "gcr.io/summary/jib-image"
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
            failureThreshold: 10
---
apiVersion: v1
kind: Service
metadata:
  name: "summary"
spec:
  selector:
    app: "summary"
  type: LoadBalancer
  ports:
    - protocol: "TCP"
      port: 8080