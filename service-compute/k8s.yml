apiVersion: apps/v1
kind: Deployment
metadata:
  name: "service-compute"
spec:
  selector:
    matchLabels:
      app: "service-compute"
  template:
    metadata:
      labels:
        app: "service-compute"
    spec:
      containers:
        - name: "service-compute"
          image: "gcr.io/service-compute/jib-image"
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
            failureThreshold: 10
---
apiVersion: v1
kind: Service
metadata:
  name: "service-compute"
spec:
  selector:
    app: "service-compute"
  type: LoadBalancer
  ports:
    - protocol: "TCP"
      port: 8080