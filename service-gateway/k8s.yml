apiVersion: apps/v1
kind: Deployment
metadata:
  name: "service-gateway"
spec:
  selector:
    matchLabels:
      app: "service-gateway"
  template:
    metadata:
      labels:
        app: "service-gateway"
    spec:
      containers:
        - name: "service-gateway"
          image: "gcr.io/service-gateway/jib-image"
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
            failureThreshold: 10
---
apiVersion: v1
kind: Service
metadata:
  name: "service-gateway"
spec:
  selector:
    app: "service-gateway"
  type: LoadBalancer
  ports:
    - protocol: "TCP"
      port: 8080