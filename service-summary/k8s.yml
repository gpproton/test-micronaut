apiVersion: apps/v1
kind: Deployment
metadata:
  name: "service-summary"
spec:
  selector:
    matchLabels:
      app: "service-summary"
  template:
    metadata:
      labels:
        app: "service-summary"
    spec:
      containers:
        - name: "service-summary"
          image: "gcr.io/service-summary/jib-image"
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
            failureThreshold: 10
---
apiVersion: v1
kind: Service
metadata:
  name: "service-summary"
spec:
  selector:
    app: "service-summary"
  type: LoadBalancer
  ports:
    - protocol: "TCP"
      port: 8080