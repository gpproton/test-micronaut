apiVersion: apps/v1
kind: Deployment
metadata:
  name: "compute"
spec:
  selector:
    matchLabels:
      app: "compute"
  template:
    metadata:
      labels:
        app: "compute"
    spec:
      containers:
        - name: "compute"
          image: "gcr.io/compute/jib-image"
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
            failureThreshold: 10
---
apiVersion: v1
kind: Service
metadata:
  name: "compute"
spec:
  selector:
    app: "compute"
  type: LoadBalancer
  ports:
    - protocol: "TCP"
      port: 8080