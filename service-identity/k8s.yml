apiVersion: apps/v1
kind: Deployment
metadata:
  name: "service-identity"
spec:
  selector:
    matchLabels:
      app: "service-identity"
  template:
    metadata:
      labels:
        app: "service-identity"
    spec:
      containers:
        - name: "service-identity"
          image: "gcr.io/service-identity/jib-image"
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
            failureThreshold: 10
---
apiVersion: v1
kind: Service
metadata:
  name: "service-identity"
spec:
  selector:
    app: "service-identity"
  type: LoadBalancer
  ports:
    - protocol: "TCP"
      port: 8080